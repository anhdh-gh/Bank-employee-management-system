// Các enum dùng chung toàn chương trình
var Enumeration = Enumeration || {};

// Các mode của form
Enumeration.FormMode = {
    Add: 1,    // Thêm mới
    Edit: 2,   // Sửa
    Delete: 3  // Xóa
}

// Loại tài khoản ngân hàng
Enumeration.BankAccountType = {
    Payment: 0, // Nữ
    Credit: 1,   // Nam
}

// Loại tài khoản ngân hàng
Enumeration.Position = {
    
}